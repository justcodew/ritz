[package]
name = "ritz"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "fitz, reforged in Rust — 高性能 PDF 处理库"

[lib]
name = "_ritz"
crate-type = ["cdylib", "rlib"]

[dependencies]
mupdf = { path = "../mupdf", version = "0.1.0" }
